package com.example.logintest.sensors;

public class AccelManager {

    // array of accel data objects
    // accel data object: ( timestamp, x, y, z )

    public boolean initAccelerometer() {
        // bring up the accelerometer

        return false;
    }

    public boolean startRecording() {

        // start recording data

        // in a thread?

        // data should be stored in the data object array

        return false;
    }

    public boolean stopRecording() {

        // kill recording

        return false;
    }

    /*
    public dataObject getAccelData() {

        // if accelData is empty, return null

        return this.dataObjectList;
    }
    */
}
